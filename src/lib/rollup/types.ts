export enum Outcome {
  Yes = "yes",
  No = "no",
}

export enum Side {
  Bid = "bid",
  Ask = "ask",
}

export enum OrderType {
  Limit = "limit",
  Market = "market",
  PostOnly = "post_only",
  ImmediateOrCancel = "immediate_or_cancel",
  FillOrKill = "fill_or_kill",
}
